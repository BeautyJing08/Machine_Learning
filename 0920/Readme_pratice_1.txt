assignment 01 is script to deal with

1. How to read csv. file to print in console

2. How to use "for loop" to find out Non data in "score" column. if it's Non data, then use "Previous column"'s data to replace

3. Then to use "for loop" to understand every student's "Type".
       if Type =="A", "revise" = "score" + 5
       elif Type =="B", "revise" = "score" +3
       else , "revise" = "score".

4. In the end , use "for loop" to understand every student's "revise".
    if "revise" > 60 , "Grade" = "Pass"
    else "Grade" = "Fail"
